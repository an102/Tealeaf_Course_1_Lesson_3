<h4>Hi <%= session[:player_name] %>!</h4>

Your cards are:
<ul>
  <% session[:player_cards].each do |card| %>
    <li><%= card.to_s %></li>
  <% end %>
</ul>
Your total is <%= calculate_total(session[:player_cards]) %>.
</br>
Your current bet is <%= session[:current_bet] %> with <%= player_cash %> remaining.